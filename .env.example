# Copy to .env (gitignored) and fill in real values. Never commit .env.

# Required for R/03_fetch_cps_org.R (CPS-ORG exposure data pull).
# Get one at https://cps.ipums.org/cps -> account -> API keys.
IPUMS_API_KEY=
